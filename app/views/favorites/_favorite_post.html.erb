<%fav = @post.fav_for(current_user)%>
<%if fav%>
  <%= link_to fa_icon("heart"), post_favorite_path(@post, fav), method: :delete, remote: true%>
<%else%>
  <%= link_to fa_icon("heart-o"), post_favorites_path(@post), method: :post, remote: true%>
<%end%>
(<%= @post.favorites.count %>)
